<?xml version="1.0" encoding="UTF-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >
    <item android:id="@android:id/progress">
        <rotate
            android:fromDegrees="270"
            android:toDegrees="270"
            android:pivotX="50%"
            android:pivotY="50%" >
            <shape
                android:innerRadiusRatio="60"
                android:shape="ring"
                android:thicknessRatio="7" >
                <gradient
                    android:centerColor="@color/colorMajor_light"
                    android:endColor="@color/colorMajor_light"
                    android:startColor="@color/colorMajor_light"
                    android:type="sweep" />
            </shape>
        </rotate>
    </item>
    <item android:id="@android:id/secondaryProgress">
        <rotate
            android:fromDegrees="270"
            android:toDegrees="270"
            android:pivotX="50%"
            android:pivotY="50%" >
            <shape
                android:innerRadiusRatio="60"
                android:shape="ring"
                android:thicknessRatio="7"  >
                <gradient
                    android:centerColor="@color/colorMajor"
                    android:endColor="@color/colorMajor"
                    android:startColor="@color/colorMajor"
                    android:type="sweep" />
            </shape>
        </rotate>
    </item>
</layer-list>